services:
  photoblog:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./photos:/app/photos
      - ./thumbnails:/app/thumbnails
      - ./photoblog.db:/app/photoblog.db
